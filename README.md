# Playwright Tests для Events Widget

## 📋 Содержание
- [Описание проекта](#-описание-проекта)
- [Структура проекта](#-структура-проекта)
- [Архитектура проекта](#-архитектура-проекта)
- [Оптимизации и рефакторинг](#-оптимизации-и-рефакторинг)
- [Конфигурационные данные](#-конфигурационные-данные)
- [Тестовые сценарии](#-тестовые-сценарии)
- [Установка и запуск](#-установка-и-запуск)
- [Отчеты и результаты](#-отчеты-и-результаты)
- [Рекомендации по разработке](#-рекомендации-по-разработке)

## 📋 Описание проекта
Проект содержит автоматизированные тесты для виджета событий (Events Widget). Тесты проверяют базовую загрузку, генерацию превью и отображение данных.

## 📁 Структура проекта
3snet-eventswidget-playwright_2/
├── fixtures/
│ └── eventswidget-fixtures.ts # Кастомные фикстуры + Page Object для виджета
├── tests/
│ ├── eventswidget.spec.ts # TC1: без выбора страны
│ └── eventswidget_with_country.spec.ts # TC2: с выбором страны
├── tests-results/ # Результаты тестов (авто-генерация)
├── .gitignore
├── package-lock.json
├── package.json
├── playwright.config.ts # Конфигурация Playwright и тестовые данные
└── README.md

## 🏗 Архитектура проекта

### 1. **Конфигурация (`playwright.config.ts`)**
- Централизованное хранение всех тестовых данных
- Настройки таймаутов, селекторов, URL
- Конфигурация браузеров и репортеров

### 2. **Фикстуры и Page Object (`fixtures/eventswidget-fixtures.ts`)**
- Инкапсуляция всех повторяющихся действий
- Методы для работы с виджетом событий
- Переиспользуемые локаторы и проверки

### 3. **Тесты (`tests/`)**
- Чистые, декларативные сценарии
- Минимум кода, максимум читаемости
- Использование методов из фикстур

## 🔧 Оптимизации и рефакторинг

### Что было вынесено в фикстуры:
- ✅ Открытие страницы (`goto`)
- ✅ Ожидание загрузки (`waitForPageLoad`)
- ✅ Выбор тематики (`selectTopic`)
- ✅ Выбор страны (`selectCountry`)
- ✅ Установка размеров (`setDimensions`)
- ✅ Выбор темы (`selectGreenTheme`)
- ✅ Генерация превью (`generatePreview`)
- ✅ Ожидание превью (`waitForPreview`)
- ✅ Проверка данных (`verifyEventRows`)

### Что было вынесено в конфиг:
- ✅ Базовый URL
- ✅ Таймауты (навигация, превью, селекторы)
- ✅ Значения селекторов
- ✅ Параметры по умолчанию (размеры, тема)

## ⚙️ Конфигурационные данные

Все тестовые данные централизованы в `playwright.config.ts`:

```typescript
export const testConfig = {
  baseUrl: 'https://dev.3snet.info/eventswidget/',
  timeouts: {
    navigation: 30000,
    preview: 55000,
    selector: 10000,
    default: 30000
  },
  selectors: {
    topic: {
      value: '10964',
      selector: '.checkselect[data-select="Выбрать тематику"]'
    },
    country: {
      selector: '.checkselect[data-select="Все страны"]',
      selectAll: 'label:has(input.selectAll)'
    },
    dimensions: {
      width: '570',
      height: '480'
    },
    theme: {
      green: 'green'
    }
  }
};
```

## 📋 Тестовые сценарии
# TC1: Базовая загрузка и генерация превью без выбора страны
1. Открытие страницы конструктора
2. Выбор тематики Igaming
3. Установка размеров (570x480)
4. Выбор зеленой темы
5. Генерация превью
6. Проверка загрузки iframe
7. Валидация данных в таблице событий (первые 3 строки)

# TC2: Базовая загрузка и генерация превью с выбором варианта страны
1. Открытие страницы конструктора
2. Выбор тематики Igaming
3. Выбор опции "Все страны"
4. Установка размеров (570x480)
5. Выбор зеленой темы
6. Генерация превью
7. Проверка загрузки iframe
8. Валидация данных в таблице событий (первые 3 строки)

🚀 Установка и запуск
## Предварительные требования
- Node.js 16 или выше
- npm или yarn
- Git

## Клонирование репозитория по HTTPS
```bash
git clone https://github.com/username/3snet-eventswidget-playwright_2.git
cd 3snet-eventswidget-playwright_2 # Переход в директорию проекта
```

## Установка зависимостей при первичном запуске клонированных тестов
```bash
npm install
```

## Установка браузеров Playwright при первичном запуске клонированных тестов
```bash
npx playwright install
```

## Запуск тестов
# Все тесты
```bash
npx playwright test
```

# Конкретные тесты
```bash
npx playwright test tests/eventswidget.spec.ts # TC1: без выбора страны
npx playwright test tests/eventswidget_with_country.spec.ts # TC2: с выбором страны
```

# Режимы запуска
```bash
npx playwright test --ui # UI режим (рекомендуется для отладки)
npx playwright test --headed # С открытым браузером
npx playwright test --reporter=html # С отчетом HTML
npx playwright test -g "TC1: Базовая загрузка" # Конкретный тест по названию
npx playwright test --trace on  # С трассировкой
```

📊 Отчеты и результаты
## Просмотр отчетов
```bash
npx playwright show-report # Открыть HTML отчет
```

## Результаты тестов
- Результаты сохраняются в папку test-results/
- Скриншоты при падении тестов (настройка screenshot: 'on')
- Трассировка при включенном флаге --trace on

🤝 Рекомендации по разработке
## При добавлении новых тестов:
1. Используйте существующие методы из фикстур
2. При необходимости добавляйте новые методы в eventswidget-fixtures.ts
3. Обновляйте конфигурацию при изменении селекторов или таймаутов
4. Следуйте существующему стилю именования (TC1, TC2...)

## При изменении существующих тестов:
1. Изменения в логике вносите в фикстуры (один раз)
2. Изменения в данные вносите в конфиг (один раз)
3. Тесты должны оставаться максимально простыми